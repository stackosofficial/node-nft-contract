Description of possible ways a function could be called and if we change path for this function in FakeRouter.

stablecoins(0) is equal to USDT on current deployed contracts.


swapExactETHForTokens: Path untouched
   matic -> WETH
   matic -> stackToken

swapExactTokensForETH: Path untouched
   unused


   ---------- FUNCTIONS THAT CHANGE PATH ----------


swapExactTokensForTokens: Path changed
   any stablecoin -> stackToken

getAmountIn: Path changed
   stablecoins(0) -> stackToken

getAmountOut: Path changed
   stackToken -> stablecoins(0)
   stablecoins(0) -> stackToken